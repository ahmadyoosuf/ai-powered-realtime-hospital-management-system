// This module is kept as a stub since the upload functionality
// has been migrated to Vercel Blob (see routes/upload.ts)
export const uploadRouter = {};
export type OurFileRouter = typeof uploadRouter;
